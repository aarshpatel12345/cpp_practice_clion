#include <iostream>
#include <vector>

int main() {
    // int n;
    // std::cin >> n;

    // int arr[n];
    // for (int i = 0; i < n; i++) std::cin >> arr[i];

    // std::vector<int> arr;
    // int value;
    //
    // // Read values dynamically
    // while (std::cin >> value) {
    //     arr.push_back(value);
    // }
    //
    // int n = arr.size();
    //
    // for (int i = 0; i < n; i++) {
    //     for (int j = 0; j < n; j++) {
    //         if (arr[i] > arr[j]) {
    //             int temp = arr[i];
    //             arr[i] = arr[j];
    //             arr[j] = temp;
    //         }
    //     }
    // }
    //
    // for (int i = 0; i < n; i++) std::cout << arr[i] << " ";

    std::cout << "hhjdsjhdf";

    return 0;
}
